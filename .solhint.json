{
  "extends": "solhint:recommended",
  "rules": {
    "compiler-version": [
      "error",
      "^0.8.9"
    ],
    "reason-string": [
      "warn",
      {
        "maxLength": 80
      }
    ]
  }
}